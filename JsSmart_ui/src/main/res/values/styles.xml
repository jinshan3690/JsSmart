<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColor">@color/textColor</item>
        <item name="android:textColorHint">@color/textColorTint</item>
        <item name="android:textSize">@dimen/textSize</item>
    </style>

    <!-- 应用启动页(StartingWindow)的theme -->
    <style name="AppTheme.StartingWindowTheme" parent="AppTheme">
        <!-- 可以设置成纯颜色（设置一个和Activity UI相似的背景） -->
        <!--<item name="android:windowBackground">@color/startingwindow_bgcolor</item>-->
        <!--也可以设置成一张图片 -->
        <!--<item name="android:windowBackground">@drawable/img_start_page</item>-->
        <item name="android:windowDisablePreview">true</item>
    </style>

    <style name="BottomNavigationActive">
        <item name="android:textSize">15sp</item>
    </style>

    <style name="BottomNavigationInactive">
        <item name="android:textSize">12sp</item>
    </style>

</resources>