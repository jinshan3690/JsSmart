<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android" >
    
    <item android:state_pressed="true">
        <shape >
            <corners android:bottomRightRadius="@dimen/btnRadius" android:bottomLeftRadius="@dimen/btnRadius"/>
            <solid android:color="@color/colorPrimary70"/>
        </shape>
    </item>
    <item>
        <shape >
            <corners android:bottomRightRadius="@dimen/btnRadius" android:bottomLeftRadius="@dimen/btnRadius"/>
            <solid android:color="@color/colorPrimary"/>
        </shape>
    </item>

</selector>
